# templatelab-crawler
Ethical metadata crawler for TemplateLab.com – discovers public template pages, extracts metadata only (no full articles or files), stores in PostgreSQL, provides FastAPI search API and CSV/JSON/Excel exports. Respects robots.txt and does not bypass protections.
